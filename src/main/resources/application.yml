spring:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
      properties:
        hibernate:
          show_sql: true
          format_sql: true
          open-in-view: false
  datasource:
    url: jdbc:mariadb://220.85.169.165:3306/interface_club
    username: ${MARIADB_USERNAME}
    password: ${MARIADB_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver

mvc:
  path-match:
    matching-strategy: ant_path_matcher